// Schema is managed via Supabase SQL Editor (see supabase-schema.sql).
// This file is kept as a reference — tables, views, and functions are
// defined in the SQL file and applied directly in the Supabase dashboard.
export const SCHEMA_NOTE = 'Schema managed by Supabase. See supabase-schema.sql.';
