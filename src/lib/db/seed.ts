// Seeding is handled via Supabase SQL Editor (see supabase-schema.sql).
// This file is kept as a no-op for backward compatibility with imports.
export function seedDatabase(): void {
  // No-op — seed data lives in supabase-schema.sql
}
